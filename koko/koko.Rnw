\documentclass[pdftex,12pt,a4paper]{article}

\input{/home/boris/science/tex_general/title_bor_utf8_knitr}


\begin{document}
\parindent=0 pt % отступ равен 0


\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Cov(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}


\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Cov(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}
\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Cov(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}
\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Cov(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}
\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Cov(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}
\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Cov(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}
\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Cov(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}
\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Cov(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}
\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Cov(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}
\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Cov(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}
\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Cov(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}
\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Cov(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}
\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Cov(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}
\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Cov(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}
\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Cov(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}
\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Cov(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}
\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Cov(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}
\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Cov(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}
\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Cov(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}
\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Cov(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}
\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Cov(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}
\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Cov(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}
\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Cov(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}
\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Cov(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}
\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Cov(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}
\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Cov(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}
\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Cov(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}
\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Cov(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}
\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Cov(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}
\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Cov(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Corr(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}


\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Corr(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}



\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Corr(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}



\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Corr(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}



\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Corr(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}



\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Corr(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}



\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Corr(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}



\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Corr(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}



\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Corr(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}



\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Corr(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}



\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Corr(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}



\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Corr(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}



\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Corr(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}



\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Corr(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}



\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Corr(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}



\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Corr(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}



\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Corr(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}



\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Corr(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}



\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Corr(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}



\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Corr(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}



\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Corr(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}



\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Corr(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}



\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Corr(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}



\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Corr(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}



\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Corr(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}



\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Corr(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}



\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Corr(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}



\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Corr(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}



\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Corr(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}



\newpage
\pagestyle{empty}

\begin{center}
\textbf{Компьютерная контрольная}
\end{center}

Группа: 

\vspace{10pt}

ФИО:

\vspace{10pt}



\begin{enumerate}
\item Величины $X$ и $Y$ независимы. Величина $X$ распределена нормально, $X\sim N(4.\Sexpr{sample(9,1)},6.\Sexpr{sample(9,1)})$, величина $Y$ распределена экспоненциально, $Y\sim exp(\lambda=2.\Sexpr{sample(9,1)})$. Используя симуляционный подход примерно посчитайте

\begin{enumerate}
\item $\P(X+Y>5.\Sexpr{sample(9,1)})$
\item $\E(X/(X+\Sexpr{sample(9,1)}Y))$
\item $\Var(XY)$
\item $\Corr(XY,X/Y)$
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Загрузите данные по стоимости квартир в Москве, \href{http://goo.gl/zL5JQ}{goo.gl/zL5JQ}, в табличку с именем \verb|h|. Обозначим буквой \verb|a| ответ на первый вопрос первой задачи. Отберите индивидуальную выборку лично для себя, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)) # здесь "a" --- это ответ на первый пункт первой задачи
h <- h[sample(1:nrow(h),1000),]
@

Постройте 90\%-ый доверительный интервал для:
\begin{enumerate}
\item Доли кирпичных домов, \verb|brick==1|
\item Доли кирпичных домов, \verb|brick==1|, среди домов находящихся близко от метро,  \verb|walk==1|
\item Разницы доли кирпичных домов среди домов расположенных близко и далеко от метро
\end{enumerate}

Место для ответов:

\vspace{50pt}


\item Сгенерируйте искусственные данные, выполнив команды:
<<eval=FALSE>>=
set.seed(round(100*a)+42) # здесь "a" --- это ответ на первый пункт первой задачи
x <- rexp(200,lambda=2)
@

Величины $X_i$ независимы и имеют функцию плотности $f(x)=e^{a-xe^a}$ при $x>0$.
\begin{enumerate}
\item Оцените неизвестный параметр $a$
\item Оцените дисперсию полученной оценки
\item Постройте 90\%-ый доверительный интервал для $a$
\item Используя результат предыдущего пункта, на 10\%-ом уровне значимости проверьте гипотезу $H_0$: $a=0.7$ против альтернативной гипотезы $H_a$: $a\neq 0.7$.
\end{enumerate}

Место для ответов:



\end{enumerate}






\end{document}